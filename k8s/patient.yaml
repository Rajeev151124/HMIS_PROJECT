apiVersion: apps/v1
kind: Deployment
metadata:
  name: patient
spec:
  replicas: 2
  selector:
    matchLabels:
      app: patient
  template:
    metadata:
      labels:
        app: patient
    spec:
      containers:
      - name: patient
        image: patient-service
        ports:
        - containerPort: 3000
